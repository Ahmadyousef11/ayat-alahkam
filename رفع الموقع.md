# رفع الموقع على الإنترنت — Cloudflare Pages

اخترنا **Cloudflare Pages** لأنه المنصّة المجانية الوحيدة التي تعطي
**تدفّقًا غير محدود** — وهذا ما يحتاجه موقع فيه ٣٤١ ميجابايت من التسجيلات.
(Netlify صار ١٥ جيجا/شهر ويوقِف الموقع عند نفادها، وGitHub Pages ١٠٠ جيجا/شهر.)

كل ملف في الموقع أقل من ٢٥ ميجابايت، وهو حدّ Cloudflare للملف الواحد. ✔

---

## الخطوات (١٠ دقائق، بلا برامج ولا أوامر)

### ١. أنشئ حسابًا
<https://dash.cloudflare.com/sign-up> — بريد وكلمة مرور فقط، بلا بطاقة.

### ٢. ادخل إلى Workers & Pages
من القائمة الجانبية: **Workers & Pages** ← **Create** ← تبويب **Pages**
← **Upload assets**.

### ٣. اختر اسم المشروع
مثلًا `ayat-alahkam` — سيكون الرابط:
`https://ayat-alahkam.pages.dev`

### ٤. ارفع المجلد
اضغط **Select from computer** ← **Upload folder**، واختر مجلد
**«موقع الدورة»** كاملًا. أو اسحب المجلد وأفلته في الصفحة.

> الرفع ٣٨١ ميجابايت — على اتصال متوسط يستغرق ١٠–٢٠ دقيقة.
> لا تغلق الصفحة حتى ينتهي.

### ٥. انشر
اضغط **Deploy site**. بعد دقيقة يظهر الرابط جاهزًا للمشاركة.

---

## التحديث لاحقًا

ادخل المشروع ← **Create deployment** ← ارفع المجلد من جديد.
يمكنك أيضًا الرجوع إلى أي نسخة سابقة من تبويب **Deployments**.

## نطاق خاص (اختياري)

إن ملكت نطاقًا مثل `motqen.com`:
المشروع ← **Custom domains** ← **Set up a domain**.
شهادة الأمان (HTTPS) تُصدر تلقائيًّا ومجانًا.

---

## ملاحظات

- ملف `_headers` يجعل المتصفح يحفظ الصوت والشرائح سنة كاملة،
  فلا تُحمَّل مرتين — يوفّر تدفقًا ويسرّع التصفّح.
- الملفات `serve.py` و `serve.bat` و `tools/` للتشغيل المحلي فقط،
  ولا تضرّ إن رُفعت (لا تُنفَّذ على الخادم).
- لا ترفع الملفات الأصلية `.m4a` ولا `.pptx` — الموقع لا يحتاجها.
