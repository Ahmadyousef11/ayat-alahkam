# دليل التعديل اليدوي

كل شيء في هذا الموقع ملفات نصية عادية. تحرّرها بأي محرّر نصوص
(**Notepad++** أو **VS Code** — لا تستخدم Word).

> **قاعدة ذهبية واحدة:** بعد أي تعديل، أعِد تشغيل `serve.bat`
> ثم اضغط في المتصفح **Ctrl + Shift + R** (تحديث بلا ذاكرة مؤقتة).
> الخادم يعيد بناء البيانات تلقائيًّا عند كل تشغيل.

---

## ٠. أين أعدّل: على الجهاز أم على الإنترنت؟

**عدّل دائمًا على الجهاز** في هذا المجلد، ثم انشر. لا تعدّل من موقع GitHub
مباشرة، وإلا تعارضت النسختان.

| | |
|---|---|
| **١. عدّل** | افتح الملف واحفظه |
| **٢. جرّب** | `serve.bat` ← <http://localhost:8000> ← **Ctrl + Shift + R** |
| **٣. انشر** | انقر **`update-site.bat`** — يرفع كل ما غيّرته دفعة واحدة |

بعد ٣ يظهر التعديل على <https://ahmadyousef11.github.io/ayat-alahkam/>
خلال دقيقة (اضغط **Ctrl + Shift + R** لترى الجديد لا المحفوظ في المتصفح).

### أشهر تعديلين

| ما تريد تغييره | الملف | ابحث عن |
|---|---|---|
| وصف الموقع في نتائج البحث وواتساب | `index.html` | `name="description"` و `og:description` |
| نصوص الصفحة الرئيسية والأرقام | `index.html` | النص كما تقرأه |
| عنوان مجلس أو وصفه | `data/course.json` | رقم المجلس |
| نصّ التفريغ | `subs/lecN.vtt` | الكلمة نفسها |

> `sync.html` أداة خاصة بك لضبط توقيت الشرائح، ولا يوجد رابط لها في الموقع
> فلا يراها الزوّار. افتحها بنفسك عند الحاجة.

---

## ١. عنوان المجلس ووصفه وتاريخه

**الملف:** `data/course.json`

ابحث عن المجلس برقمه وعدّل ما بين علامتي التنصيص فقط:

```json
{
  "id": 5,
  "title": "﴿كُتِبَ عَلَيْكُمُ الصِّيَامُ﴾",      ← العنوان في البطاقة وأعلى الصفحة
  "subtitle": "مفتتح آيات الصيام",            ← السطر الذهبي تحت العنوان
  "summary": "بلاغة النداء…",                 ← الوصف التشويقي في البطاقة
  "date": "٧ يوليو",                          ← يظهر بجانب العنوان
  "audio": "audio/lec5.mp3",
  "pdf": "pdf/lec5.pdf",
  "slides": 15,                               ← عدد الشرائح (لا تغيّره يدويًّا)
  "duration": 7853                            ← مدة التسجيل بالثواني
}
```

**احترس:** لا تحذف الفاصلة `,` بين الحقول، ولا تضع فاصلة بعد الحقل الأخير.
إن أخطأت، سيخبرك الخادم بالسطر عند التشغيل.

---

## ٢. توقيت الشرائح (الأهمّ)

التوقيتات الحالية استُخرجت آليًّا، ودقّتها جيدة لا كاملة.

### الطريقة السهلة — محرّر المزامنة

1. افتح `sync.html` مباشرة (أداة خاصة بك، ولا رابط لها في الموقع)
2. اختر المجلس، واضغط **«تحميل التوقيت المحفوظ»** لتبدأ من الموجود
3. شغّل الصوت — ويمكنك رفع السرعة إلى ٢× أو ٣×
4. كلّما انتقل الشيخ إلى شريحة جديدة اضغط الزر الذهبي أو مفتاح **M**
5. اضغط **«تنزيل الملف»** واحفظه في `data/timings/` مكان الملف القديم

### الطريقة الأدقّ — تعديل رقم واحد

**الملف:** `data/timings/lec5.json` — مصفوفة أرقام، كل رقم = ثانية بداية الشريحة:

```json
[0, 529.1, 672.4, 1016.9, 1290.2, ...]
```

الرقم الأول للشريحة ١، والثاني للشريحة ٢، وهكذا. لتأخير الشريحة ٣
إلى الدقيقة ١٢ اكتب `720` مكان `672.4`.

> الأرقام **يجب أن تكون تصاعدية**، وعددها = عدد الشرائح بالضبط.
> لمعرفة التوقيت أثناء الاستماع: الوقت ظاهر تحت الشريحة، ومكتوب على كل مصغّرة.

---

## ٣. نصّ التفريغ

**الملف:** `subs/lec5.vtt`

```
WEBVTT

12
00:01:23.000 --> 00:01:31.000
النص المعروض في هذا المقطع
```

عدّل النص بحرية. ولتصحيح توقيت مقطع غيّر أرقام السطر الذي فيه `-->`
بالصيغة `ساعة:دقيقة:ثانية.أجزاء`. لا تحذف كلمة `WEBVTT` من أول الملف،
وأبقِ سطرًا فارغًا بين المقاطع.

---

## ٤. الألوان والخطوط

**الملف:** `assets/css/style.css` — أول ٤٠ سطرًا فقط:

```css
:root {
  --navy:    #14263f;   /* الكحلي الأساسي */
  --gold:    #c9a227;   /* الذهبي */
  --gold-lt: #e3c86b;   /* ذهبي فاتح — العناوين */
  --cream:   #f7f2e5;   /* خلفية الصفحة */
  --radius:  16px;      /* استدارة الحواف */
  --maxw:    1200px;    /* أقصى عرض للمحتوى */
}
```

وتحت `html[data-theme="dark"]` ألوان الوضع الليلي.
لتكبير خط الموقع كله: ابحث عن `font-size: 18px` في `body`.

---

## ٥. الصفحة الرئيسية

**الملف:** `index.html` — النصوص مكتوبة مباشرة، عدّلها كما تقرأها:

```html
<h1>آيات الأحكام من سورة البقرة</h1>
<p class="sub">قراءة بيانية وفقهية</p>
<p class="who">🎙️ مع <b>فضيلة الأستاذ الدكتور مصطفى حسنين</b></p>

<div class="stats">
  <div><b>٩</b><small>مجالس</small></div>       ← الأرقام الأربعة
  <div><b>١٣٨</b><small>شريحة</small></div>
</div>
```

---

## ٦. استبدال شريحة أو صورة بطاقة

- **شريحة:** احفظ الصورة الجديدة مكان `slides/5/07.webp`
  بنفس الاسم والامتداد `.webp` ونسبة ١٦:٩.
  (للتحويل من PNG: <https://squoosh.app> — اختر WebP جودة ٨٥)
- **صورة البطاقة** في الصفحة الرئيسية: `img/thumb5.webp` بعرض ٦٤٠ بكسل.

> احترس من الامتداد المزدوج مثل `07.webp.PNG` — لن يُحمَّل الملف.

---

## ٧. إضافة مجلس جديد

1. `audio/lec10.mp3` · `pdf/lec10.pdf` · `slides/10/01.webp` … بالترقيم المتتالي
2. `img/thumb10.webp` صورة البطاقة
3. أضف كتلة المجلس في `data/course.json` (انسخ كتلة سابقة وعدّلها)
4. `data/timings/lec10.json` — مصفوفة أصفار مؤقتًا، ثم اضبطها من `sync.html`
5. `subs/lec10.vtt` إن توفّر تفريغ
6. حدّث أرقام الإحصائيات في `index.html`

---

## ٨. قبل النشر — قائمة تحقّق

- [ ] شغّل `serve.bat` وتأكّد من عدم ظهور رسائل خطأ في نافذة الخادم
- [ ] افتح المجالس التسعة، وتحقّق من العنوان والصوت والشرائح والتفريغ
- [ ] اختبر التقديم والتأخير والانتقال بين الشرائح
- [ ] جرّب الوضع الليلي وقياس الجوال (`F12` ثم `Ctrl+Shift+M`)
- [ ] تأكّد من عمل زرَّي التحميل (MP3 و PDF)

**نسخة احتياطية:** انسخ مجلد الموقع كاملًا قبل أي تعديل كبير.

---

## إن تعطّل شيء

| العَرَض | السبب الغالب |
|---|---|
| البطاقات لا تظهر | خطأ صياغة في `data/course.json` — راجع نافذة الخادم |
| شريحة بيضاء | اسم الملف أو امتداده غير مطابق |
| التفريغ لا يظهر | فتحت `index.html` مباشرة بدل `serve.bat` |
| لا يمكن القفز في الصوت | شغّلت `python -m http.server` بدل `serve.py` |
| التعديل لا يظهر | لم تحدّث بـ `Ctrl+Shift+R`، أو الخادم لم يُعَد تشغيله |
